# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

This repository (`archie-redford/dfg`) is currently empty: there is no
source code, no build tooling, no dependency manifest, and no test suite.
The `master` branch has no commits. Nothing in this file is inferred or
guessed — it reflects the literal current state of the repo.

Because there is no codebase yet, there are no build/lint/test commands,
no architecture, and no conventions to document. Do not invent any of
these for the user; ask what they want to build, or look for instructions
elsewhere in the conversation/task description.

## When code is added

Once a project is scaffolded into this repo, replace this section (and
this file generally) with the real specifics, for example:

- Language/framework and how dependencies are installed
- Build command(s)
- How to run the full test suite and a single test
- Lint/format commands
- High-level architecture (major modules/services and how they interact)
- Branching and commit conventions actually used in this repo

Keep this file in sync with the project as it evolves — update it whenever
workflows, structure, or conventions change materially, rather than letting
it go stale.
